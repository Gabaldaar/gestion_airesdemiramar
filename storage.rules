rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow anyone to read images.
      allow read;
      // Allow authenticated users to write (upload/delete) images.
      allow write: if request.auth != null;
    }
  }
}
