
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow public read access so anyone can see the images.
      allow read;
      // Allow write access (uploads, deletes) only to authenticated users.
      allow write: if request.auth != null;
    }
  }
}
